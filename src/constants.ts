export enum LogLevel {
    DEBUG = "debug",
    INFO = "info",
    WARNING = "warning",
    ERROR = "error",
    DISABLED = "disabled",
}

export enum BatchLimitType {
    COUNT = "count",
    SIZE = "size",
}